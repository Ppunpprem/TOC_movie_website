import React from "react";
import ReactDOM from "react-dom/client";
import MovieIntelligence from "./MovieIntelligence";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MovieIntelligence />
  </React.StrictMode>,
);